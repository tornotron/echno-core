/**
 * @module api-services
 *
 * Re-exports the full public API of the `api-client` module.
 * Downstream consumers should import from `@echno-core/lib/api` rather
 * than addressing the individual client file directly.
 */
export * from './api-client';
