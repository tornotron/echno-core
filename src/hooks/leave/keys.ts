/**
 * @module hooks/leave/keys
 *
 * Re-exports the {@link leaveKeys} query-key factory, which is defined
 * alongside the query hooks in `use-leave.ts`.
 */
export { leaveKeys } from './use-leave';
